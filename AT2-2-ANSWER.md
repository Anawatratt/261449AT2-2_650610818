AT2-2: Integration Testing & Automation – Answer Document

Student ID: 650610818

1. สิ่งที่ค้นพบว่าระบบไม่เป็นไปตามที่คาดหวังไว้ หรือปัญหาที่ไม่คิดว่าจะเจอ

จากการทำ Integration Testing พบพฤติกรรมของระบบหลายจุดที่ไม่ชัดเจนตั้งแต่แรก โดยเฉพาะเมื่อทดสอบหลาย component ร่วมกันจริง ๆ เช่น EmailService มีการ catch exception แล้ว pass ทำให้ระบบยังถือว่าคำสั่งซื้อสำเร็จแม้ส่ง email ไม่ได้ ส่งผลให้ลูกค้าอาจถูกตัดเงินแต่ไม่ได้รับการยืนยันคำสั่งซื้อ

นอกจากนี้ ระบบไม่มีการจัดการกรณีที่ payment สำเร็จแล้วแต่ email ล้มเหลว เช่น การ refund หรือบันทึกสถานะค้างส่ง email ทำให้ติดตามปัญหาได้ยาก ในส่วนของ ShippingService (TH) พบว่าค่าจัดส่งแบ่งเพียงสองช่วงน้ำหนัก ซึ่งไม่สะท้อนต้นทุนจริง และ PaymentGateway ไม่ตรวจสอบ currency ที่รับเข้ามา อาจทำให้เกิดปัญหาเมื่อใช้งานจริง

อีกประเด็นคือ InventoryRepository.release() สามารถเพิ่ม stock เกินจริงได้ หากถูกเรียกซ้ำโดยไม่มีการ reserve มาก่อน และระบบยังไม่มี order_id หรือ transaction log ที่ชัดเจน ทำให้การตรวจสอบย้อนหลังทำได้จำกัด

2. การทดสอบโค้ดนี้แบบอัตโนมัติ มีข้อดี ข้อเสียอะไรบ้าง

ข้อดี คือสามารถรันเทสต์ได้รวดเร็วและสม่ำเสมอ ลดhuman eror ช่วยตรวจจับ regression bug และทำให้มั่นใจมากขึ้นในการแก้ไขหรือ refactor โค้ด อีกทั้งสามารถนำไปใช้ร่วมกับ CI เพื่อป้องกันการ merge โค้ดที่มีปัญหาได้

ข้อเสีย คือใช้เวลาในการเขียนและดูแลเทสต์พอสมควร โดยเฉพาะ integration test ที่มีหลาย dependency นอกจากนี้ coverage สูงไม่ได้หมายความว่าโค้ดไม่มี bug และบางเทสต์อาจ fail ได้จากปัจจัยที่ควบคุมยาก

3. ขั้นตอนการทำให้เทสต์รันอัตโนมัติอย่างเป็นระบบที่ทำในกิจกรรมนี้

เริ่มจากติดตั้ง pytest และตั้งค่า marker เพื่อแยกประเภทการทดสอบ จากนั้นเขียน test ตามระดับ integration (top-down, bottom-up, sandwich) โดยใช้ stub และ spy ให้เหมาะสม เมื่อเทสต์ในเครื่องผ่านแล้วจึงตั้งค่า GitHub Actions ให้รัน pytest อัตโนมัติทุกครั้งที่มีการ push หรือ pull request พร้อมวัด coverage เพื่อใช้ประเมินความครอบคลุมของเทสต์

4. ชุดทดสอบตั้งต้น ได้ coverage เท่าไร เมื่อเพิ่มกรณีทดสอบแล้ว ได้ coverage เท่าไร

ชุดทดสอบตั้งต้นมี coverage ประมาณ 60–70% ครอบคลุมเฉพาะ main flow หลังจากเพิ่มกรณีทดสอบรวมประมาณ 27 tests coverage เพิ่มขึ้นเป็นประมาณ 85–95% โดยครอบคลุม error cases, boundary conditions และหลาย region มากขึ้น ส่วนที่ยังไม่ครอบคลุมคือ code ที่ถูก catch แล้ว ignore และกรณีที่เกิดได้ยากในสถานการณ์จริง

5. เป็นไปได้ไหมที่จะทำให้ได้ 100% integration test coverage

ในทางปฏิบัติ การทำให้ได้ 100% integration test coverage เป็นเรื่องที่ ยากเนื่องจากบางส่วนของโค้ดไม่สามารถ trigger ได้ง่ายในสภาพแวดล้อมทดสอบ และการไล่ coverage จากระดับสูงให้ถึง 100% ใช้ต้นทุนสูงแต่ได้ประโยชน์เพิ่มไม่มาก ดังนั้นควรตั้งเป้า coverage ประมาณ 80–90% และเน้นทดสอบ logic ที่สำคัญต่อระบบมากกว่า